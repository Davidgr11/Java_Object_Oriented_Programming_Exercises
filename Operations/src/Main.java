import javax.swing.JOptionPane;

public class Main {

	public static void main(String[] args) {
		/*String nombre;
		int resp;
		do {
			nombre=JOptionPane.showInputDialog("Escribe tu nombre");//regresa string
		
			System.out.println(nombre);
			resp=JOptionPane.showConfirmDialog(null, "Estas seguro?");//regresa el boton elegido 0, 1 o 2
			System.out.println(resp);
		}while(resp==1);
		if(resp==0) {
			JOptionPane.showMessageDialog(null, "Bienvenido"+nombre);
		}
		*/
		/*
		double cantidad;
		JOptionPane.showMessageDialog(null, "Ingresando al convertidor de pesos a dolares");
		cantidad=Double.parseDouble(JOptionPane.showInputDialog("Dame la cantidad en pesos mexicanos"));
		cantidad=cantidad/19.94;
		cantidad=cantidad*10;
		Math.round(cantidad);
		cantidad=cantidad/10;
		JOptionPane.showMessageDialog(null, "En dolares serían: $"+cantidad);
		JOptionPane.showMessageDialog(null, "Deberias comprar dolares");*/
		Pantalla pantalla=new Pantalla();
	
	}

}
